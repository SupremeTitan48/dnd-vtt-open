# Contributing

## Principles
- Keep modules bounded; avoid cross-layer imports.
- Write tests for behavior changes.
- Prefer small pull requests with clear rationale.

## Local workflow
1. Install dev dependencies.
2. Run `pytest` and `python -m ruff check .`.
3. Update docs when interfaces or content schema changes.
